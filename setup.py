from setuptools import setup, find_packages

setup(
    name="ragflows",
    version="0.1.0",
    packages=['ragflows'],
    package_dir={'ragflows': 'ragflows'},
    install_requires=[
        # 添加项目依赖（如果有）
    ],
)
